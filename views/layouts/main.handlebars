<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <style>
        body{font-family: sans-serif; background-color: #f4f4f4 ;}
        header{background-color: black; color: antiquewhite; padding: 10px;}
        form { margin: 12px 0; }
        input, button { padding: 8px; margin: 6px 6px 6px 0; }
        button { cursor: pointer; border: 1px solid #000; background: #fff; }
    </style>
    <title>Backend E-Commerce</title>
</head>
<body>
    {{>header}}

    <main>{{{ body }}}</main>

</body>
</html>
